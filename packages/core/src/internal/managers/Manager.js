import { HeleonixComponent } from '../HeleonixComponent';

export class Manager extends HeleonixComponent {
  #cachedItems = new Map();

  get items() {
    return
  }

  create(name) {}

  delete(name) {}
}
